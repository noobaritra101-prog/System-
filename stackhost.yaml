User:
  name: "Aritra"
  github: "system-"

runtime:
  image: "python:3.11-slim"

commands:
  package: ""
  build:
    - "pip install -r requirements.txt"
  start: "uvicorn system:app --host 0.0.0.0 --port 8000"

repository:
  branch: "main"
  auto_deploy: true
  
