User:
  name: "Aritra"
  github: "system-"

runtime:
  image: "python:3.11-slim"

commands:
  package: ""
  build:
    - "pip install -r requirements.txt"
  # --- CHANGED BACK TO PYTHON ---
  start: "python main.py" 

repository:
  branch: "main"
  auto_deploy: true
